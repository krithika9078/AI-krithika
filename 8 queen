N=8
def safe(b,r,c):
    for i in range(r):
        if b[i]==c or abs(b[i]-c)==r-i: return False
    return True
def solve(r=0,b=[]):
    if r==N: print(b); return True
    for c in range(N):
        if safe(b,r,c) and solve(r+1,b+[c]): return True
solve()
